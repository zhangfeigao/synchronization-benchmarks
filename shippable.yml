#http://docs.shippable.com/ci/cpp-continuous-integration/
language: c

compiler:
  - gcc

runtime:
  nodePool: shippable_shared_aarch64

# use this to control what branches get built.
branches:

# using pre-defined build variables
# full list http://docs.shippable.com/ci/advancedOptions/environmentVariables/
env:
  global:
    - TEST_RESULTS_DIR=$SHIPPABLE_REPO_DIR/shippable/testresults

build:

  # http://docs.shippable.com/ci/shippableyml/#ci
  ci:
    - cd benchmarks/lockhammer && make
    - cd cripts && ./runall.sh >> $TEST_RESULTS_DIR/lockhammer.log


# Integrations are used to connect external resources to CI
# http://docs.shippable.com/integrations/overview/
integrations:

  # http://docs.shippable.com/ci/shippableyml/#notifications
  notifications:
  # turning of email for PR builds, get notified only on failure and change in status
  # http://docs.shippable.com/integrations/notifications/email/
    - integrationName: email
      type: email
      recipients:
        - zhangfei.gao@foxmail.com
